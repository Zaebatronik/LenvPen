# 🚀 Инструкция по запуску проекта «Лень-в-Пень»

## 📋 Предварительные требования

- Node.js 18+ установлен
- Аккаунт Supabase (бесплатный tier подойдёт)
- Telegram Bot Token (получить у @BotFather)
- Git установлен

---

## 1️⃣ Настройка Supabase

### 1.1 Создание проекта

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Сохраните:
   - `SUPABASE_URL` (Project URL)
   - `SUPABASE_ANON_KEY` (anon public key)
   - `SUPABASE_SERVICE_ROLE_KEY` (service_role secret key)

### 1.2 Применение миграций

1. Откройте SQL Editor в Supabase Dashboard
2. Скопируйте содержимое файла `database/migration_001_initial.sql`
3. Вставьте и выполните запрос
4. Проверьте, что все таблицы созданы

---

## 2️⃣ Настройка Backend

```bash
cd backend
npm install
cp .env.example .env
```

Отредактируйте `.env`:

```env
PORT=3000
NODE_ENV=development

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

TELEGRAM_BOT_TOKEN=your-bot-token
WEBAPP_URL=http://localhost:5173

TZ=Europe/Moscow
```

Запуск:

```bash
npm run dev
```

Backend запустится на `http://localhost:3000`

---

## 3️⃣ Настройка Frontend

```bash
cd frontend
npm install
cp .env.example .env
```

Отредактируйте `.env`:

```env
VITE_API_URL=http://localhost:3000/api
```

Запуск:

```bash
npm run dev
```

Frontend запустится на `http://localhost:5173`

---

## 4️⃣ Настройка Telegram Bot

### 4.1 Создание бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и сохраните токен

### 4.2 Настройка WebApp

1. У @BotFather отправьте `/mybots`
2. Выберите вашего бота
3. Выберите "Bot Settings" → "Menu Button"
4. Укажите URL: `http://localhost:5173` (для разработки)

### 4.3 Запуск бота

```bash
cd bot
npm install
cp .env.example .env
```

Отредактируйте `.env`:

```env
TELEGRAM_BOT_TOKEN=your-bot-token-here
WEBAPP_URL=http://localhost:5173
```

Запуск:

```bash
npm start
```

---

## 5️⃣ Тестирование локально

### Для Telegram WebApp

Telegram WebApp можно тестировать только через HTTPS. Варианты:

**Вариант 1: ngrok (рекомендуется для разработки)**

```bash
# Установите ngrok
# https://ngrok.com/download

# Запустите туннель для frontend
ngrok http 5173

# Запустите туннель для backend (в другом терминале)
ngrok http 3000
```

Обновите:
- В `frontend/.env` → `VITE_API_URL` на ngrok URL backend
- В `bot/.env` → `WEBAPP_URL` на ngrok URL frontend
- У @BotFather Menu Button URL на ngrok URL frontend

**Вариант 2: Деплой на сервер сразу**

---

## 6️⃣ Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "Открыть приложение"
4. Пройдите регистрацию и анкету
5. Заполните дневной отчёт

---

## 🐛 Решение проблем

### Backend не запускается

```bash
# Проверьте, что порт 3000 свободен
netstat -ano | findstr :3000

# Проверьте логи
npm run dev
```

### Frontend не подключается к API

- Проверьте `VITE_API_URL` в `.env`
- Проверьте CORS настройки в backend
- Откройте DevTools → Network в браузере

### Telegram WebApp не открывается

- Убедитесь, что используется HTTPS (или ngrok)
- Проверьте Menu Button URL у @BotFather
- Проверьте, что `WEBAPP_URL` правильный в `bot/.env`

### Ошибки базы данных

- Проверьте, что миграция применена
- Проверьте RLS политики в Supabase
- Проверьте `SUPABASE_SERVICE_ROLE_KEY`

---

## 📦 Деплой на production

### Backend

Рекомендуем: **Railway**, **Render**, **Fly.io**

```bash
# Пример для Railway
railway login
railway init
railway add
railway up
```

### Frontend

Рекомендуем: **Vercel**, **Netlify**, **Cloudflare Pages**

```bash
cd frontend
npm run build

# Деплой dist/ папки на хостинг
```

### Bot

Можно запустить на том же сервере, что и backend, или отдельно:

```bash
# PM2 для production
npm install -g pm2
pm2 start index.js --name lenvpen-bot
pm2 save
pm2 startup
```

---

## 📝 Дальнейшая разработка

### Приоритеты после MVP

1. Расширенные уточняющие вопросы для зависимостей
2. Графики прогресса
3. Геймификация (бейджи, награды)
4. Мультиязычность (EN)
5. Push-уведомления через бота
6. Flutter приложение

### Структура для новых фич

```
backend/src/
  routes/       # API endpoints
  services/     # Бизнес-логика
  utils/        # Утилиты

frontend/src/
  pages/        # Страницы
  components/   # Компоненты
  services/     # API клиенты
  store/        # State management
```

---

## 💬 Поддержка

Если возникли вопросы:
1. Проверьте логи в консоли
2. Проверьте .env файлы
3. Проверьте документацию Supabase/Telegram

Успешного запуска! 🚀
