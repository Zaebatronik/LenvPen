===============================================
  ИНСТРУКЦИЯ: ВЫПОЛНИТЬ SQL МИГРАЦИЮ
===============================================

1. Откройте Supabase SQL Editor:
   https://supabase.com/dashboard/project/ypgjlfsoqsejroewzuer/sql

2. Нажмите "New Query"

3. Скопируйте ВСЁ содержимое файла:
   database/migrations/002_update_schema_full_c3_o3.sql

4. Вставьте в SQL Editor

5. Нажмите "RUN" (или Ctrl+Enter)

6. Проверьте успешное выполнение:
   - Должно быть "Success" зелёным
   - Функции get_windows_counts и calculate_current_weight созданы
   - Индексы созданы
   - app_config обновлён

===============================================
  ЧТО ДЕЛАЕТ МИГРАЦИЯ:
===============================================

✅ Добавляет колонки:
   - user_dependencies.last_slip_at
   - dependency_history.percent_before/after
   - dependency_history.weight_before/after

✅ Создаёт функции:
   - get_windows_counts(uuid, date) → P3/P7/P30, W3/W7/W30
   - calculate_current_weight(...) → текущий вес C3

✅ Обновляет app_config:
   - coefficients (все коэффициенты C3/O3)
   - dependency_thresholds (пороги для evaluators)
   - avatar_stages (5 стадий аватара)

✅ Создаёт индексы для производительности

===============================================
  ПОСЛЕ МИГРАЦИИ:
===============================================

Система готова к работе! 🚀

Можно тестировать:
1. Регистрация → Survey с параметрами
2. DailyReport → Worker обработает данные
3. Dashboard → покажет реальные метрики

Frontend: http://localhost:5174/
Bot: запустить с npm start
Backend: запустить с npm run dev
